server:
  port: 8082
spring:
  application:
    name: CarPickKia1
  mail:
    host: smtp.gmail.com
    port: 587
    username: CarPick152@gmail.com
    password: fweimxbigydxuvjb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    export:
      prometheus:
        enabled: true